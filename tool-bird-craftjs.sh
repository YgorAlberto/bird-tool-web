for target in $(cat target.txt);do cat OUT-WEB-BIRD/$target/$target-URL* >> OUT-WEB-BIRD/$target/$target-FULL-URLs && cat OUT-WEB-BIRD/$target/$target-FULL-URLs | sort -u >> OUT-WEB-BIRD/$target/$target-FULL-URLs-clr  && python3 bird-craftjs.py OUT-WEB-BIRD/$target/$target-FULL-URLs-clr && mv output-craftjs.txt OUT-WEB-BIRD/$target/$target-bird-crafjs;done
